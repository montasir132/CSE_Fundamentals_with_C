#include<stdio.h>
int main()
{
    // int a=57, b=217;
    // int sum = a + b;
    // int sub= a - b;
    // int mul= a * b;
    // float div= b / a;
    // int mod= b % a;
    // printf("summation= %d\nsubtraction= %d\nmultiple= %d\ndivision= %f\nmod= %d\n", sum, sub, mul, div, mod);
    
    
    float a, b;
    scanf("%d %d", &a, &b); 
    float div;
    div= b / a;
    printf("%f", div);


    
    return 0;
}